description: Documentation site for the auth-service project. Hosts the public API reference and integration guides for the JWT authentication surface.
role: docs
tags:
    - documentation
    - markdown
source:
    provider: file
    config:
        location: /opt/docs-repo
knowledge:
    sources:
        - scope: project
          provider: file
          config:
            location: knowledge
changelog:
    provider: file
    config:
        directory: changelog
