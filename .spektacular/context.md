# Context: 000034_spec-plan-implement-reconciliation

## Planning session (plan workflow, started 2026-07-09)

- Overview step complete: read spec `000034_spec-plan-implement-reconciliation.md` in full. It confirms the two-guardrail shape (pre-implementation plan-vs-spec coverage check, post-implementation implementation-vs-spec reconciliation folded into the completion report) and adds explicit constraints: additive only (no new workflow/command), must not require the user to manually open the spec file, must work for resumed/interrupted workflows, never auto-fixes gaps (surfaces + asks user only), never hard-blocks on a gap.
- Non-goals confirmed: no retroactive fix of spec 000032 itself; no coverage enforcement on the spec-writing workflow; no auto-generating plan content to close a gap.
- Technical Approach section explicitly defers exact step placement to this planning process — candidates named in the spec: pre-check could join plan's `14-verification.md` or sit at the start of implement's `analyze`; post-check could be its own step or fold into implement's existing final report step. Should also consider reusing the plan's phase-by-phase implementation record (already used for changelog) as the comparison source for the post-check, rather than re-deriving "what was built" from scratch.
- Discovery complete. research.md written to `.spektacular/work/spec-plan-implement-reconciliation/research.md` with full findings. Key decisions carried forward:
  - **Pre-implementation coverage check**: extend two existing gate steps, not add a new FSM step. `templates/steps/plan/14-verification.md` (plan-creation-time gate) and `templates/steps/implement/01-read_plan.md` (implement-start-time gate, catches hand-edited plans too — this is why both are needed, not just one). Both already have `spec_path` available via `strategy.PathVars` with zero new plumbing. `01-read_plan.md` already has the exact STOP-and-multi-option-prompt UX to extend.
  - **Post-implementation reconciliation**: one new step `reconcile_spec` inserted between `update_feature_changelog` and `finished` in the implement FSM (`Src: ["update_feature_changelog"]`, and retarget `finished`'s `Src` to `["reconcile_spec"]`). Template modeled closely on `10-update_feature_changelog.md`'s shape (gather source → author/apply → stage-then-commit via CLI → STOP-on-mismatch → advance). Renumber `11-finished.md` → `12-finished.md` (precedent: the `10→11` rename done for the changelog feature). `11-finished.md`'s "What to do next" section gets extended to report spec completion status (satisfied/unsatisfied+reason), sourced by re-reading the spec file `reconcile_spec` just rewrote — no new Go-side data threading needed.
  - **Checkbox mechanics**: `spec file read/write/list` already exists (`cmd/file.go:8-11`, shared `newStoreFileCmd` builder) — no new CLI plumbing needed. No Go code anywhere parses `- [ ]`/`- [x]` checkboxes today; this is agent-driven judgment in template prose, not mechanical Go parsing (consistent with how `10-update_feature_changelog.md` already delegates similar judgment to the agent).
  - **Branching mechanism**: no new Go-level primitive. Follows the existing multi-source-FSM-edge + template-prose pattern (`07-update_changelog.md` Step 3 is the canonical example: agent inspects state, issues one of two `goto` commands).
  - **Open question carried into architecture step**: where does "don't re-flag an accepted gap" get recorded — a marker written into the plan document itself (current lean, since plans/specs are the source of truth here, not hidden state) vs. `state.json`'s `data` field (mechanism exists via `internal/workflow/data.go` but seen elsewhere in the codebase only for transient per-run flags, not durable cross-session decisions). Needs a decision in the architecture/data-structures step.
  - **Also carried over**: whether `reconcile_spec` should be its own step vs. folded into `update_feature_changelog` — leaning separate step (cleaner STOP-on-mismatch semantics, two different artifacts being written) but not fully settled; revisit if architecture step finds a cleaner shape.
- User flagged mid-session: only `spec new` clears `.spektacular/context.md` (`internal/steps/spec/steps.go:83-89`) — `plan new`/`implement new` never do. Confirmed by direct code inspection. This context.md was trimmed on 2026-07-09 to drop pre-session content now superseded by research.md's independently-cited evidence. **Follow-up for after this plan lands** (explicitly out of scope for spec 000034, do not fold in): consider whether `plan new`/`implement new` should also clear/reset context.md the way `spec new` does.
- Architecture step complete. User made both locked decisions explicitly (not the recommended default on the first):
  - **Pre-check location: implement's `read_plan` ONLY** (not also plan's `verification`, contrary to my initial recommendation). Single insertion point — checks whatever the plan currently says, whether freshly generated or hand-edited, at the moment implementation starts. Simpler than maintaining the same comparison logic in two workflows.
  - **Post-check location: new dedicated `reconcile_spec` step** between `update_feature_changelog` and `finished` (this matched my recommendation).
  - **Accepted-gap recording: written into the plan document itself** (not `state.json` data) — confirmed via architecture.md's reasoning (plans/specs are the durable record in this codebase, not opaque workflow state).
- architecture.md and conventions.md written to `.spektacular/work/spec-plan-implement-reconciliation/`. No project conventions apply (knowledge base has no populated convention entries yet — only README placeholders).
- Components step complete. components.md written. Five components: `read_plan` (extended), a lightweight descoped-requirement marker convention within the plan document itself (not a new file/store), `reconcile_spec` (new step, sole writer to the spec store during implement), `finished` (extended), and the existing spec/plan stores reused as-is.
- Data structures step complete. No new Go types/interfaces — pure `workflow.StepConfig` slice addition (`reconcile_spec` between `update_feature_changelog` and `finished`) plus markdown template content. No new template variables needed anywhere (spec_path/plan_name/changelog_section_name already exist). Descoped-marker format left open for the Implementation Detail step to pin down exactly.
- Implementation detail step complete. Confirms: gate extension (not duplication) in read_plan; branch-by-template-prose (no new Go control flow) for both guardrails; reconcile_spec mirrors update_feature_changelog's 4-part shape; descoped marker is plain plan-document prose, no new store/parsing layer; finished renumbers to 12-finished.md (git mv, precedent from prior changelog feature).
- Dependencies step complete. No new external libraries; no changes needed to internal/workflow, internal/stepkit, or the plan/spec workflow packages. Only internal/steps/implement package is touched. Modeled directly on the already-landed 000033 changelog plan.
- Testing approach step complete. Unit tests cover FSM wiring + template-instruction content (mirroring steps_test.go's existing string-assertion pattern); judgment halves of all 3 success metrics are explicitly classified Manual/test-plan since checkbox-satisfaction judgment is agent-driven, not deterministic — matches how update_changelog's deviation summarization is already (not) tested.
- Milestones step complete, confirmed by user. Milestone 1 = pre-implementation coverage gate (read_plan extension). Milestone 2 = post-implementation reconciliation (reconcile_spec + finished extension).
- Phases step complete, confirmed by user (4 phases, 2 per milestone). phases_plan.md and phases_context.md written with concrete file:line detail:
  - Phase 1.1: extend `templates/steps/implement/01-read_plan.md` with new Step 3.5 (spec coverage check, two-option STOP prompt), insert between existing Step 3 (drift check, lines 40-59) and Step 4 (changelog mode detection, lines 61-66). No Go callback changes.
  - Phase 1.2: descoped-requirement marker format (`**Descoped requirements**:` list in plan.md), same template edit as 1.1, staged/committed via existing `plan file write` pattern.
  - Phase 2.1: new `reconcile_spec` StepConfig in `internal/steps/implement/steps.go` between `update_feature_changelog` (line 28) and `finished` (line 29); new `templates/steps/implement/11-reconcile_spec.md` modeled on `10-update_feature_changelog.md`; rename `11-finished.md`→`12-finished.md` via git mv.
  - Phase 2.2: extend renamed `12-finished.md`'s Summary + "What to do next" sections to report spec completion status via `spec_path` (already available, no new plumbing).
- Open questions step complete: none. Everything resolved during discovery/architecture.
- Out of scope step complete: 3 spec Non-Goals + 4 architecture decisions explicitly excluded (verification-step duplicate check, folding into update_feature_changelog, new Go branching primitive) + the deferred context.md-clearing follow-up.
- Assemble step complete. Feature slug confirmed: 000034_spec-plan-implement-reconciliation. All 3 documents staged to .spektacular/tmp/ (plan_template.md, context_template.md, research_template.md). During assembly, caught and fixed a staleness bug in research.md: it still described the original (superseded) dual-location pre-check proposal from discovery, contradicting the locked architecture decision (implement's read_plan only). Fixed by adding a new "Alternatives considered and rejected" bullet for the dual-location option, rewriting the "Chosen approach" section to lead with the locked decision and mark the original proposal as superseded, and updating two Open Assumptions bullets from "assumed" to "settled" (descoped-marker-in-plan-doc; reconcile_spec as separate step) to match the user's explicit architecture-step confirmations.
- Verification step complete. All 25 required sections (11 plan.md + 7 context.md + 7 research.md) present, in order, filled. No shell commands in plan.md. All 4 phase Technical-detail anchors resolve correctly to matching context.md Phase headings. All 6 research.md alternatives have file:line citations. No fixes needed — staged documents pass as assembled.
- write_plan step complete: plan.md committed to store via `plan file write`, scratch file removed, confirmed via read-back.
- write_context step complete: context.md committed to store, scratch file removed.
- write_research step complete: research.md committed to store, scratch file removed, working directory .spektacular/work/spec-plan-implement-reconciliation/ removed. All three plan documents (plan.md, context.md, research.md) now live in the plan store.
- Plan workflow finished. All 3 documents live at `.spektacular/plans/spec-plan-implement-reconciliation/`. No further steps.
