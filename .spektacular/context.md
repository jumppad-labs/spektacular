# Working Context: implement workflow for plan 000040_plan-draft-then-walkthrough

## Workflow state
- Active workflow: **implement** for plan 000040 (draft-then-walkthrough planning flow). Started fresh after the plan workflow closed (all three docs `status: completed`).
- read_plan gate: PASSED. All 10 plan.md sections present; 5 phase checkboxes (1.1, 1.2, 2.1, 2.2, 2.3) all unchecked with resolving context.md anchors; drift check found zero mismatches (all files/symbols/line anchors verified current); spec coverage complete — no descoped requirements needed.
- Changelog mode: **first-phase invocation** — no `## Changelog` in plan.md yet; analyze picks up at Phase 1.1.

## Phase map (from plan.md)
- Phase 1.1: insert `walkthrough` FSM step (steps.go slice + `walkthrough()` writeStep callback), git mv 18-finished.md→19-finished.md, new 18-walkthrough.md (lifted walkthrough prose, mandatory, sign-off gate), slim 19-finished.md, update steps_test.go order/walk/wiring.
- Phase 1.2: spek-plan SKILL.md loop+resume updates, context_directive_test floor 29→30, harbor instruction.md + test_plan_workflow.py step order (:599, :894), `go run . init` to refresh installed skill copies.
- Phase 2.1: de-gate gathering templates 02–12, add shared assumption-log + proceed-unless-blocked block, rewrite TestArchitectureStepContainsOptionsAndAgreementBeat, extend work_files_test for assumptions.md.
- Phase 2.2: scaffold research.md `## Drafting assumptions` section, 13-assemble mapping row (missing assumptions.md ≠ STOP), 14-verification 7→8 sections, walkthrough assumptions beat, SKILL.md working-file contract.
- Phase 2.3: harbor E2E — zero-confirmation-question test, populated-assumptions test, status-lifecycle test, instruction.md success criteria, solve.sh.

## Key verified anchors (drift check, this session)
- `internal/steps/plan/steps.go`: Steps() slice at :31-53, `writeStep` helper :67, `finished()` :287-324 renders `steps/plan/18-finished.md` at :324.
- `internal/workflow/state.go:25` InProgress(); `workflow.go:184-186` same-step Goto re-render, `renderStep` :243, `commitTerminal` :264-268, before_ callbacks :115-123.
- `cmd/resume.go`: detectInProgress :87, resumeOrClear :123, guardKind :153. `cmd/storefile.go` set-status :266. `cmd/implement.go` plan-exists stat gate ~:119.
- `templates/steps/plan/18-finished.md:18-36` holds the walkthrough prose to lift; `02-discovery.md:42-43` gate; `templates/steps/implement/01-read_plan.md:73-79` STOP-shape to copy.
- `internal/steps/plan/steps_test.go`: TestArchitectureStepContainsOptionsAndAgreementBeat :55, TestStepsOrderMatchesExpected :102, TestFSMWalkFromNewToFinished :131, finished tests :203-343.
- `templates/context_directive_test.go`: marker const :17, floor 29 at :95-96. Harbor: test_steps_executed_in_order :599, test_no_unfilled_scaffold_slots :612, plan-file-write test :648, test_every_rendered_next_step_is_valid :894. Makefile harbor-test-plan :73.

## Phase progress
- **Phase 1.1 COMPLETE** (checkbox + all 4 ACs ticked, changelog entry written 2026-07-31, verify all green: build/vet/test/lint). Delivered: walkthrough FSM step (steps.go), 18-walkthrough.md (mandatory review, marker×1), 19-finished.md (renamed+slimmed, marker×0), steps_test.go extended (TestWalkthroughWiring, TestWalkthroughStepIsMandatoryGuidedReview, TestPlanFinishedSuccessBranchHasNoWalkthroughOffer + order/walk lists).
- **Phase 1.2 COMPLETE** (checkbox + 3 ACs ticked, changelog entry written, verify all green). Note: harbor cannot exercise a withheld sign-off — instruction.md tells the harness agent to act as the reviewing user and sign off itself.
- **Phase 2.1 COMPLETE** (checkbox + 4 ACs ticked, changelog written, verify all green incl. spec-workflow-untouched spot-check).
- **Phase 2.2 COMPLETE** (checkbox + 3 ACs ticked, changelog written, verify all green).
- **Current phase: 2.3** — harbor E2E: instruction.md success criteria (zero user-directed confirmation questions during drafting; assumption log populated; docs in-progress until sign-off, completed after); test_plan_workflow.py new tests: (a) no drafting-step transcript segment poses a user-confirmation question (open question: reliable detection signal — if none exists, STOP and ask whether to narrow to uninterrupted-step-progression or weaker proxy), (b) research.md has filled `## Drafting assumptions` with Decision/Rationale/Rejected entries, (c) status lifecycle (in-progress at walkthrough, completed only after finished); solve.sh update if it enumerates steps; run via `make harbor-test-plan` (Makefile:73) — NOTE harbor suite not runnable locally in this session; py_compile + collect-only is the local check.
- 2.3 analysis (harbor research): OPEN QUESTION RESOLVED — reliable question signal exists: AskUserQuestion tool_use blocks are in raw JSONL (archived run 2026-06-03 shows one posed at architecture, harness errors it "Answer questions?" is_error) but extract_tool_calls whitelist (py:282, only Bash/Skill/Task/Agent) drops them — extend whitelist + use resolve_step_windows (py:381) to assert zero AskUserQuestion in pre-walkthrough windows. Prose-question detection deliberately skipped (assistant text not captured, agent self-answers — brittle); note as gap. Status lifecycle: NO existing frontmatter helpers in test file (plan drift, minor — plan said "extend existing helpers"); add small ---block parser; assert final 3 artefacts `status: completed` + walkthrough-window `plan file read` result shows `status: in-progress`. solve.sh:296-301 MISSING walkthrough goto (FSM would reject write_research→finished) — add before finished; also verify assemble goto present. state.json has no per-step timestamps; transcript ordering (rendered_steps pattern py:935) is the timing signal. Archived artefacts predate metadata subsystem (no YAML frontmatter) — don't trust them as oracles.
- 2.3 implement DONE: solve.sh += assemble goto (pre-existing gap: out_of_scope→verification is an invalid transition; fixed while touching the file) + walkthrough goto before finished (bash -n OK); instruction.md += draft-straight-through bullet and three success criteria (no AskUserQuestion during drafting; populated § Drafting assumptions with decision/rationale/rejected; in-progress during walkthrough → completed after finished). Python test additions delegated to test step.
- 2.3 tests DONE (sub-agent): extract_tool_calls whitelist += AskUserQuestion (consumer audit clean; note TestAgentExecution::test_agent_did_work now technically passable by AskUserQuestion-only run — harmless, untouched); new TestAutonomousDrafting (no-confirmation-questions per pre-walkthrough window; drafting-assumptions populated-or-explicit-empty) + TestArtefactStatusLifecycle (completed after finished; in-progress observed via plan-file-read results in walkthrough window) + hand-rolled parse_frontmatter. py_compile clean, 93 tests collect. Harbor suite RUN locally (harbor CLI + docker + creds all present).
- 2.3 verify: local checks all green (build/vet/test/py_compile/collect 93/solve.sh syntax+ordering). First harbor E2E run (job 2026-07-31__09-18-43): AgentTimeoutError at 900s — BUT the flow itself worked: agent drove new→assemble with ZERO AskUserQuestion calls (autonomous drafting proven); it was mid-assemble at cutoff, never a stall. Deviation: raised tests/harbor/plan-workflow/task.toml [agent] timeout_sec 900→1800 (budget predates the walkthrough step) and re-ran — second run in progress at session cut (background task bm6k44nvl). If resuming: check the newest tests/harbor/jobs/*/plan-workflow__*/verifier/test-stdout.txt for the pytest results before ticking Phase 2.3.
- Second harbor run (job 09-37-23): 63 passed / 29 failed — ALL new-flow assertions passed (zero AskUserQuestion during drafting, walkthrough-before-finished, in-progress during walkthrough); the 29 failures share one root cause, PRE-EXISTING drift: store's missing_id_prefix validation (spec.id_method=timestamp, from the earlier store/artifact-metadata work) rejects `plan file write user-auth/...` because the seeded spec `user-auth.md` has no timestamp prefix — in-container agent improvised `20260731083901-user-auth`, so artefacts landed off-path. Fix applied (harness alignment): environment spec renamed to `20260101000000-user-auth.md` (git mv + Dockerfile COPY), instruction.md + solve.sh references sed'd (test_plan_workflow.py is name-agnostic via state.json data.name — no change needed). Third harbor run (job 10-03-03): 92/93 passed — every new-flow + artefact + status-lifecycle assertion green. Sole failure: TestConventionAwarePlanning::test_conventions_read_during_discovery — stale hand-maintained oracle CONVENTIONS_READ_COMMAND="knowledge conventions"; the discovery template switched to `knowledge always-applied` in an earlier plan and run-3 transcript confirms the agent ran it during discovery (sibling seeded-convention-token test passed). Fix applied: oracle + module docstring + instruction.md:39 → "knowledge always-applied". Fourth harbor run (job 10-24-54): 91/93 — the two failures were harness-robustness issues, both fixed: (a) SCAFFOLD_LEFTOVERS literal `<description>` false-positived on the agent's legit JSON error-envelope prose → replaced with distinctive `### Option A: <name>`; (b) test_docs_in_progress_during_walkthrough's precondition (≥1 plan file read in walkthrough window) flaked because 18-walkthrough.md said read "as you need them" → template now mandates reading all three docs at walkthrough start ("ground the review in the committed state"; also right for resume). Local go tests + py_compile green after both. Fifth (confirmation) run launched tracked — check newest job verifier/test-stdout.txt; across runs every assertion has passed in ≥1 run, so treat a novel content-quality flake as reportable, not a blocker. Fifth run: **93/93 PASSED** — full green E2E. Phase 2.3 ticked (+3 ACs), changelog entry written with all deviations, ALL 5 PHASES COMPLETE. Repo CHANGELOG.md gained the 000040 release-note section (prepended). test-plan.md committed (3 manual procedures: walkthrough-revision check, no-section-approval observation, downstream-quality watch incl. the research.md 8-section open question). Feature changelog record committed to changelog store. Spec reconciled: all 15 checkboxes (8 requirements + 7 ACs) flipped to [x] — every one judged satisfied against the plan changelog; ACs 3 (blocking-question stop) and 5 (live comment revision) are mechanism-delivered and template-test-enforced, with live observation captured in test-plan.md. Remaining: finished step only.
- 2.2 analysis: scaffold/research.md — insert `## Drafting assumptions` between `## Open assumptions` (:36) and `## Rehydration cues` (:41); 13-assemble.md:37 research mapping line (reword "seven sections") + :39 STOP paragraph gains assumptions.md exception ("No drafting assumptions were recorded" line, not STOP); 14-verification.md :39-46 research list — insert Drafting assumptions as 7, Rehydration cues → 8; 02-discovery.md:49 "seven sections" refers to discovery's own research working file — correct as-is, leave; 18-walkthrough.md add beat 4 (read research.md § Drafting assumptions, walk entries, challenge = change request via apply-immediately path); SKILL.md working-files paragraph + loop reinforcement; scaffold_test.go currently tests plan.md only — research-shape test is new.
- 2.2 implement DONE (inline): scaffold research.md `## Drafting assumptions` (between Open assumptions and Rehydration cues); 13-assemble research mapping now two bullets (research.md + assumptions.md → § Drafting assumptions) + missing-assumptions exception ("No drafting assumptions were recorded." line, not STOP); 14-verification research list 7→8 entries; 18-walkthrough beat 4 (walk entries, challenge = change request); SKILL.md draft-straight-through paragraph + assumptions.md in working-file contract + resume read-back; init claude re-run (installed copy has assumptions.md ×2). Build clean, markers intact.
- 2.2 tests DONE (sub-agent, resumed once after API cut): TestResearchScaffoldShape (8 headings in order); TestAssembleStepMapsAssumptionLog; TestVerificationStepRequiresDraftingAssumptions; TestWalkthroughStepWalksDraftingAssumptions; work_files_test TestAssemblyStepsMapSectionFiles += assumptions.md. go test ./... green.
- 2.1 analysis (gate catalog, verified): 02:L43 present-summary (L42 ask-only-what-code-can't stays, L47 knowledge gate stays); 03: L5-14 options block (keep analysis, drop present-to-user), L16-23 Get Agreement (→ choose & record as leading assumption), L36 "agreed", L42 conventions propose-to-user (→ record in log), L46 advance gate; 04/05/06/07/08: "Present it to the user for review" + "agreed" + "Once the user is happy with ..." at (15,17,19)/(15,17,19)/(27,29,31)/(15,17,19)/(36,38,40); 09: L15 "by asking the user", L18 agreed, L20 present+agreed advance (gate AFTER save); 10: L46 present+agreed advance (no "agreed" in save); 11: L7 "asking the user" scope rule, L16 "ask the user now" example, L27 agreed, L29 agree advance (L25 implementer-STOP guidance stays); 12: L18 present+confirm-each, L20 agreed, L22 agree advance. All share byte-identical context footer. work_files_test structure read: planGathering list :38-50, cleanup exactness :91-109, assemble mapping :124-131.
- 2.1 shared block design (identical prose in all 02-12, inserted before the advance paragraph): "**Record your judgement calls.**" → append to `.spektacular/work/{{plan_name}}/assumptions.md`, entry `### <short decision title> (<step name>)` / Decision / Rationale / Rejected; "**Proceed unless genuinely blocked.**" → no review stops; no-reasonable-default or user-only info → STOP, options in one block, don't advance past dependent point; "Otherwise proceed without interruption." Test anchors: "assumptions.md", "proceed without interruption", absence of "Present it to the user for review"/"Once the user is happy"/"Once the user has agreed"/"Once the user agrees".
- 2.1 implement DONE (two parallel agents, 02-07 / 08-12): all gates removed, agreed→drafted, shared block inserted byte-identically (md5 66d7d91585d9 across all 11), knowledge gate + "Ask only questions the code cannot answer" (02) and implementer-STOP guidance (11) preserved, go build clean. 2.1 tests DONE: TestArchitectureStepContainsOptionsAndAgreementBeat → TestArchitectureStepWeighsOptionsAndRecordsAssumption (option + assumption log + NotContains "agreement"); new TestGatheringStepsProceedWithoutApprovalGates (11 callbacks, 7 banned phrases oracle); work_files_test planGathering loop also requires assumptions.md (spec steps exempt). go test ./... all green.
- Autonomy: driving phases without pausing per stored user preference (skip confirmation prompts, stop only for real decisions).
- 1.2 analysis: EXPECTED_STEP_ORDER at test_plan_workflow.py:61 (add walkthrough before finished); EXPECTED_SKILLS_PER_STEP needs no walkthrough entry (template references no skills); test_steps_executed_in_order asserts completed==EXPECTED_STEP_ORDER (order covered), still add rendered-instruction-level finished-after-walkthrough test per plan; instruction.md has no explicit step list — add walkthrough to prose + success criteria; skill_resume_test binds SKILL.md edits (keep .spektacular/work/, context.md, --force, resumable, no state.json); after SKILL.md edit run `go run . init claude` to refresh installed copies.
- 1.2 implement DONE: SKILL.md — walkthrough added to loop examples, new "workflow ends with a mandatory walkthrough review" paragraph, resume guidance covers current_step=walkthrough (work files gone → plan file read); instruction.md — walkthrough bullet (harness agent acts as reviewing user, signs off itself) + finished-only-after-walkthrough success criterion; `go run . init claude` refreshed .claude/skills/spek-plan/SKILL.md. 1.2 tests DONE (sub-agent): floor 29→30 (comment now 33−1 non-terminal), EXPECTED_STEP_ORDER += walkthrough, new TestInstructionNextStepValidity::test_walkthrough_rendered_before_finished (transcript-level ordering). go test ./templates/ + ./internal/steps/plan/ green; py_compile + pytest --collect-only (89 tests) clean.

## Phase 1.1 learnings
- 17-write_research.md's goto footer renders `{{next_step}}` from the callback arg — Go-side retarget was enough, no template edit.
- `templates/templates.go` embeds `all:*` — new templates auto-registered. Resume/status-lifecycle deliberately not re-asserted (covered by workflow_test.go TestGotoSameStepReRenders + existing finished() tests).

## Open questions carried from the plan (STOP-and-ask if hit)
- Harbor "agent asked a confirmation question" detection feasibility (Phase 2.3) — if no reliable transcript signal, STOP and ask whether to narrow the assertion.
- Any implement-side hard-coding of research.md's 7-section list — if found, STOP and ask (implement workflow out of scope).

## Learnings (carried from plan session)
- `templates/scaffold/context.md` lacks `## Project References` but 14-verification.md requires it — hand-added at assemble; candidate knowledge entry (user was to be offered at review).
- Assumption-log entries use `### ` headings so naive `^## ` parsers don't mistake them for document sections.

## User answers (this implement session)
- User chose plan 000040_plan-draft-then-walkthrough to implement.
