description: The auth-service repo hosting the JWT authentication implementation for the auth-service project.
role: service
tags:
    - go
    - auth
    - jwt
source:
    provider: file
    config:
        location: ..
knowledge:
    provider: file
    config:
        location: knowledge
changelog:
    provider: file
    config:
        directory: changelog
